import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { registerUser } from "../services/api"; import { Logo } from "../components/Navbar";
function Register(){const navigate=useNavigate();const [form,setForm]=useState({name:"",email:"",phone:"",password:"",role:"user",location:""}),[error,setError]=useState(""),[loading,setLoading]=useState(false); const submit=async(e)=>{e.preventDefault();setLoading(true);setError("");try{await registerUser(form);navigate('/login',{state:{message:'Account created. Please sign in.'}})}catch(err){setError(err.message)}finally{setLoading(false)}};const roles=[['user','Customer','I need help for home or local work.'],['Farmer','Farmer','I need farm labour, a tractor driver, or other farm help.'],['Worker','Worker','I offer my skills and want to receive job requests.']];return <main className="auth-page register-layout"><div className="auth-aside"><Logo/><div><p className="eyebrow">Join your community</p><h1>Good work<br/>belongs <em>close</em> to home.</h1><p>Create an account to find help, offer your skills, and strengthen your local economy.</p></div><div className="aside-quote">Local connections. Lasting possibilities.</div></div><section className="auth-panel"><Link className="auth-mobile-logo" to="/"><Logo/></Link><form className="auth-card register-card" onSubmit={submit}><p className="eyebrow">Create account</p><h2>Let’s get you started.</h2>{error&&<p className="form-error">{error}</p>}<div className="form-grid"><label>Full name<input placeholder="Your name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Phone number<input type="tel" placeholder="Your number" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} required/></label><label>Email address<input type="email" placeholder="you@example.com" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Location<input placeholder="Village / town" value={form.location} onChange={e=>setForm({...form,location:e.target.value})} required/></label></div><label>Password<input type="password" placeholder="Create a password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label><fieldset><legend>Choose your account type</legend><p className="role-help">This can be changed later by an administrator.</p><div className="role-options">{roles.map(([value,title,description])=><label key={value} className={form.role===value?'selected':''}><input type="radio" name="role" value={value} checked={form.role===value} onChange={e=>setForm({...form,role:e.target.value})}/><span><b>{title}</b><small>{description}</small></span></label>)}</div></fieldset><button className="button full" disabled={loading}>{loading?'Creating account…':'Create account'} <span>→</span></button><p className="auth-switch">Already have an account? <Link to="/login">Sign in</Link></p></form></section></main>};export default Register;
