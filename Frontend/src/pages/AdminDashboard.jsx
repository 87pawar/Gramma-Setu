import Navbar from "../components/Navbar";import { useAuth } from "../context/AuthContext";function AdminDashboard(){const {user}=useAuth();return <><Navbar/><main className="dashboard-shell container"><p className="eyebrow">Admin console</p><h1>Hello, {user?.name?.split(' ')[0]}.</h1><p className="dashboard-sub">Manage the community and keep Grama Setu trusted.</p><div className="dash-actions"><div><b>Users</b><span>Manage accounts</span></div><div><b>Workers</b><span>Verify profiles</span></div><div><b>Requests</b><span>Monitor activity</span></div></div><section className="dash-section empty-state"><strong>Administration tools are ready.</strong><p>Use the backend admin endpoints to manage users, workers and requests.</p></section></main></>};export default AdminDashboard;
