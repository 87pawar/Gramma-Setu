import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { loginUser } from "../services/api"; import { useAuth } from "../context/AuthContext"; import { Logo } from "../components/Navbar";
function Login(){const {login}=useAuth(), navigate=useNavigate(); const [form,setForm]=useState({email:"",password:""}),[error,setError]=useState(""),[loading,setLoading]=useState(false); const submit=async(e)=>{e.preventDefault();setLoading(true);setError("");try{const data=await loginUser(form);login(data.token,data.user);navigate('/dashboard',{replace:true})}catch(err){setError(err.message)}finally{setLoading(false)}}; return <main className="auth-page"><div className="auth-aside"><Logo/><div><p className="eyebrow">Welcome back</p><h1>Your community<br/><em>works</em> together.</h1><p>Sign in to manage your work, requests, and local connections.</p></div><div className="aside-quote">“The right help is now only a few taps away.”</div></div><section className="auth-panel"><Link className="auth-mobile-logo" to="/"><Logo/></Link><form className="auth-card" onSubmit={submit}><p className="eyebrow">Sign in</p><h2>Good to see you.</h2><p className="form-intro">Enter your details to continue to Grama Setu.</p>{error&&<p className="form-error">{error}</p>}<label>Email address<input type="email" placeholder="you@example.com" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Password<input type="password" placeholder="Your password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label><button className="button full" disabled={loading}>{loading?'Signing in…':'Sign in'} <span>→</span></button><p className="auth-switch">New to Grama Setu? <Link to="/register">Create an account</Link></p></form></section></main>}; export default Login;
