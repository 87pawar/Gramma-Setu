function HowItWorks() { const steps=[['01','Search nearby','Choose a service and discover professionals working close to you.'],['02','Choose with confidence','See clear profiles, skills, rates, availability and community ratings.'],['03','Request & relax','Send a request directly and keep track of every job in one place.']]; return <section className="how-it-works"><div className="container"><div className="section-heading centered"><p className="eyebrow">How it works</p><h2>A better way to get things done.</h2><p>Bringing useful skills and everyday needs together, right where they matter.</p></div><div className="steps-container">{steps.map(([number,title,description])=><article className="step-card" key={number}><span>{number}</span><h3>{title}</h3><p>{description}</p></article>)}</div></div></section> }
export default HowItWorks;
